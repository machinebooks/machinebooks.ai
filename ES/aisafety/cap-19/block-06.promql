# Extraido de: LibroAISafety/cap-19-observabilidad.md
(
  sum by (tool_name) (
    rate(ai_security_tool_invocations_total[1h])
  )
  -
  sum by (tool_name) (
    rate(ai_security_tool_invocations_total[1h] offset 7d)
  )
)
/
sum by (tool_name) (
  rate(ai_security_tool_invocations_total[1h] offset 7d)
)
