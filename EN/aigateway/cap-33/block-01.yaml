# Extracted from: LibroAIGateway/cap-33-stack-nginx.md
# docker-compose.yml — persistent volumes
volumes:
  mysql_data:
    driver: local
  redis_data:
    driver: local
  qdrant_data:
    driver: local
